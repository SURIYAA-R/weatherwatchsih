# Python 3.14+ compatible — install with:
# pip install --only-binary :all: -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database — Supabase (PostgreSQL) in production, SQLite for local dev
sqlalchemy>=2.0.30
asyncpg>=0.30.0          # async PostgreSQL driver (for Supabase)
aiosqlite>=0.20.0        # fallback for local SQLite dev
psycopg2-binary>=2.9.9   # sync PostgreSQL driver (for Alembic migrations)

python-multipart>=0.0.9
pydantic>=2.9.0
pydantic-settings>=2.5.0
httpx>=0.27.0
python-dateutil>=2.9.0
geopy>=2.4.1
faker>=25.0.0
python-dotenv>=1.0.0     # load .env file
